# DebugHUD Setup

1. Canvas 아래 Text 생성 (`DebugText`)
2. Canvas 또는 별도 UI 오브젝트에 `DebugHUD` 컴포넌트 부착
3. `hudText`에 `DebugText` 연결
4. 나머지 참조는 자동 탐색 또는 수동 연결

표시 항목:
- STAMINA
- WEIGHT(kg)
- RISK
- CARGO(%)
